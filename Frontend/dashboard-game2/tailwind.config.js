theme: {
  extend: {
    colors: {
      ring: 'hsl(var(--ring))', // add this if using ring tokens
      border: 'hsl(var(--border))',
      background: 'hsl(var(--background))',
      foreground: 'hsl(var(--foreground))',
    },
  },
},
